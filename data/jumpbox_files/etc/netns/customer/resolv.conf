# empty on purpose.  This file will be filled out by dhclient inside the customer network namespace if/when called by detect.sh
# detect.sh itself is called by the systemd service cust_rules.service (/etc/systemd/system/cust_rules.service)
# if you're debugging this, good luck :D